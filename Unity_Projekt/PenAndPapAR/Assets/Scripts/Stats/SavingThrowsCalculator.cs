using UnityEngine;

namespace Stats
{
    public class SavingThrowsCalculator : MonoBehaviour
    {
        public GameObject modifierGameObject;
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
